[pytest]
testpaths = tests
# The tests import from `src`, which only resolves if the repository root is on sys.path.
# Running pytest from the root happens to work locally because the working directory lands
# there anyway; on CI it does not, so state it explicitly rather than rely on the accident.
pythonpath = .
filterwarnings = ignore::DeprecationWarning
